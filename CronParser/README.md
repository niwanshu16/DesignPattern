This is a simple Command Line Application which takes cron expression as input and output the parsed values for each fields.
Run the application from the Executor.java file which contains main method.
Provide the Expression as input and it will return the values for each field (minute,hour,day of month, month, day of week)
To Exit the application, give input as exit.